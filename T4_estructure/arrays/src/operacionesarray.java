import java.util.Scanner;

public class operacionesarray {
    public static void main(String[] args) {
       /* Scanner lecturaTeclado = new Scanner(System.in);
        System.out.println("Cuantos numeros quieres generar");
        int longitus = lecturaTeclado.nextInt();
        int[] array1 = new int[10];int [] array2= new int[longitus];
        for (int i = 0; i < longitus; i++) {
            int aleatorios = (int) (Math.random() * 10) + 1;
            array1[i] = aleatorios;
            aleatorios = (int) (Math.random() * 10) + 1;
            if (array1[i]%2 ==0){
               int suma = array1[i]+1;
            }else if (array1[i]%2 !=0){
                int resta = array1[i]-1;
            }
            int aleatorios2= (int) (Math.random()*10)+1;
            array2[i]=aleatorios2;
            aleatorios2 = (int) (Math.random()*10)+1;
            if (array2[i]%2 ==0){
                int suma = array2[i]+1;
            }else if (array2[i]%2 !=0){
                int resta = array2[i]-1;
            }
        }
        System.out.print(array2[i]+" ");
        System.out.print(array1[i]+" ");

        for (int i = 0; i <array1.length ; i++) {
            int aleatorio = (int) (Math.random()*6)*5;
            array1[i] = array1[i]+aleatorio;
        }
        int ultimo = numeros[numeros.lenght-1];
        for (int i = numeros.length-1; i >0 ; i--) {
            numeros[i]=numeros[i-1];
        }
        for (int i = 0; i < array2.length/2; i++) {
            int n = array2[i];
            array2[i] = array2[array2.length - 1 - i];

        }
        int poscionpar=0;
        for (int i = 0; i < array2.length; i++) {
            if (array2[i]%2 !=0) {
               int  posicionimpar=i;
            }
        }*/
    }







}
