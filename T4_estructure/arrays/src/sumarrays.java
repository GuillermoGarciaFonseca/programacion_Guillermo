import java.util.Scanner;

public class sumarrays {
    /*public static void main(String[] args) {
        Scanner lecturaTeclado = new Scanner(System.in);
        System.out.println("Cuantos numeros quieres generar");
        int longitus = lecturaTeclado.nextInt();
        int[] numeros1= new int[longitus];
        int [] numeros2 = new int[longitus];
        int [] numeros3 = new int[longitus];

        for (int i = 0;i<longitus;i++){
          int   aleatorios= (int) (Math.random()*10)+1;
          numeros1[i]= aleatorios;
          aleatorios = (int) (Math.random()*10)+1;
          numeros2 = (int)(Math.random()*10)+1;

        }

    numeros1 =rellenarArray(numeros1);
    numeros2 =rellenarArray(numeros2);
    numeros3=rellenarArray(numeros2);
        for (int i = 0; i < ; i++) {
            
        }
    }
    private static void rellenarArray(int[] array){
        for (int i = 0; i < array.length; i++) {
            int aleatorio = (int) (Math.random()*10)+1;
            array[i] = aleatorio;
        }
        return array;
    }*/
}
